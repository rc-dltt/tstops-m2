# Filename of the Test Data(CSV)
data.filename=uat-data
# Number of Threads(users)
thread.size=150
# Ramp-Up Period(seconds)
thread.rampup=1
# Duration(seconds)
thread.duration=1800
# Startup Delay(seconds)
thread.startup_delay=1